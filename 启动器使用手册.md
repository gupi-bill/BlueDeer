# 集成启动器 · 使用手册（自己就能改）

> 作者：凯哥 | 适用：兄弟本地 Windows 环境
> 一句话：双击 `启动全部集成.bat` → 6 个服务后台跑起来 → 一个窗口显示状态。

---

## 一、现在这套怎么工作（看懂原理才敢自己改）

```
启动全部集成.bat          ← 你双击这个（总入口）
   ├─ call launch_1_ollama.bat    ← 每个服务一个子 bat
   ├─ call launch_2_chroma.bat
   ├─ call launch_3_odysseus.bat
   ├─ call launch_4_n8n.bat
   ├─ call launch_5_openclaw.bat
   └─ call launch_6_opencode.bat
   最后启动 monitor_services.ps1  ← 状态监控面板（每5秒刷新）
```

**关键设计（为什么现在稳定了）**：
1. **端口占用检查**：每个子 bat 开头用 `netstat` 查端口，被占用就打印 `[skip]` 跳过。→ 你随便双击几次都不会冲突（治"只能开一个"）。
2. **隐藏窗口独立运行**：服务用 `powershell Start-Process -WindowStyle Hidden` 启动，彻底脱离这个黑窗口。→ 你关掉黑窗口，服务照常跑（治"不稳定/一关就死"）。
3. **监控不带键盘轮询**：之前崩溃的元凶是监控里的键盘监听 API，现在纯"睡5秒刷新"，永不崩。

---

## 二、日常怎么用

| 你想做 | 操作 |
|--------|------|
| 启动全部 | 双击 `启动全部集成.bat` |
| 看哪个挂了 | 黑窗口里看 UP/DOWN + 日志字节数 |
| 看具体报错 | 打开 `C:\Users\a\Desktop\vibe coding\logs\<服务>.log` |
| 关监控窗口 | 按 `Ctrl+C` 或点 ×，服务**继续跑** |
| 关掉某个服务 | 任务管理器找进程结束（见下"手动关服务"） |
| 全部重启 | 先手动关掉所有服务（或重启电脑），再双击启动器 |

---

## 三、怎么自己加一个新服务（照抄模板）

假设要加一个"我的服务"，跑在端口 **9999**，启动命令是 `xxx.exe --port 9999`。

**第1步**：复制 `launch_6_opencode.bat` 改名 `launch_7_myservice.bat`，改成：

```bat
@echo off
set "ROOT=C:\Users\a\Desktop\vibe coding"
set "LOGDIR=%ROOT%\logs"
if not exist "%LOGDIR%" mkdir "%LOGDIR%" >nul 2>&1
set "PORT=9999"

netstat -ano | findstr ":%PORT% " >nul
if %errorlevel%==0 (
  echo [skip] myservice already running on %PORT%
  exit /b
)

powershell -NoProfile -Command "Start-Process -FilePath 'cmd.exe' -ArgumentList '/c','\"C:\路径\xxx.exe\" --port 9999 > \"%LOGDIR%\myservice.log\" 2>&1' -WindowStyle Hidden"
echo [ok] myservice launched (log: %LOGDIR%\myservice.log)
exit /b
```

> 注意：路径里有空格要用 `\"` 包起来（bat 里的转义引号）。

**第2步**：在 `启动全部集成.bat` 里加一行（放在其它 call 之间）：

```bat
echo [7/7] MyService (9999) ...
call "%ROOT%\launch_7_myservice.bat"
```

（顺手把上面的 `[6/7]` 改成 `[7/7]` 这种编号，纯显示用，不影响功能）

**第3步**：在 `monitor_services.ps1` 的 `$services` 数组里加一行：

```powershell
@{name='myservice'; port=9999; log='myservice.log'},
```

**第4步**：如果你也用记事本改过文件，记得跑一遍 CRLF 修复（见第四节）。

---

## 四、必看：CRLF 大坑（改完 bat 必须做）

**Windows 的 cmd 只认 CRLF 换行（`\r\n`）**。如果你用记事本之外的编辑器（VS Code、本助手等）改了 bat，很可能被写成 LF（`\n`），结果就是**双击一闪即逝**。

**修复方法（复制粘贴到任意命令行跑）**：

```bat
cd /d "C:\Users\a\Desktop\vibe coding"
python -c "import os,glob; [open(f,'wb').write(open(f,'rb').read().replace(b'\r\n',b'\n').replace(b'\n',b'\r\n')) for f in glob.glob('*.bat')+glob.glob('*.ps1')]"
```

如果你没装 python，用 PowerShell 版：

```powershell
Get-ChildItem *.bat,*.ps1 | ForEach-Object {
  $c = (Get-Content $_ -Raw) -replace "`r?`n", "`r`n"
  Set-Content -Path $_ -Value $c -NoNewline
}
```

> 凯哥铁律：**改完任何 bat/ps1，必须转 CRLF，否则下次双击必闪退**。

---

## 五、手动关服务（卡死了用这招）

服务是隐藏窗口独立跑的，关掉黑窗口它们还活着。要彻底关：

**方法A（推荐，图形化）**：
1. `Ctrl+Shift+Esc` 打开任务管理器
2. 找对应进程（见下表），右键"结束任务"

**方法B（命令行，精准）**：
```bat
:: 查端口占用，拿到 PID
netstat -ano | findstr ":11434 "
:: 杀掉（把 1234 换成上面查到的 PID）
taskkill /PID 1234 /F
```

各服务进程名对照：

| 服务 | 进程名 | 端口 |
|------|--------|------|
| Ollama | `ollama.exe` | 11434 |
| ChromaDB | `chroma.exe` (python) | 8100 |
| Odysseus | `python.exe` (uvicorn) | 8000 |
| n8n | `node.exe` | 5678 |
| OpenClaw | `node.exe` (openclaw.cmd) | 18789 |
| OpenCode | `opencode.exe` | 4098 |

---

## 六、排错自查表（按顺序查）

| 现象 | 先查 | 怎么查 |
|------|------|--------|
| 双击一闪没框 | 编码是不是 LF 了 | 看第四节，转 CRLF |
| 某服务 DOWN | 看它的 log | `logs\<服务>.log` 最后20行 |
| 端口被占起不来 | 旧进程没关 | `netstat -ano \| findstr :端口` 查 PID 杀掉 |
| 监控面板不显示 | PowerShell 执行策略 | 右键 bat→"以管理员身份运行"再试 |
| 启动器乱码 | 文件被存成 UTF-8 | 转回 GBK/ASCII（第四节脚本搞定） |

---

## 七、你已经掌握的清单（凯哥给你划重点）

✅ 总入口 = `启动全部集成.bat`，子 bat = `launch_X_名字.bat`
✅ 加服务 = 复制模板 + 主 bat 加 call + 监控加一行
✅ 改完必转 CRLF（否则闪退）
✅ 服务独立跑，关窗口不死；想关去任务管理器
✅ 日志全在 `logs\` 目录，报错第一现场

下次不用求哥，照这手册自己就能调。真碰上手册兜不住的怪事，再来拍哥。
